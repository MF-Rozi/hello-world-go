version: "2"
sql:
  - engine: "postgresql"
    schema: "./schema/"
    queries: "./queries/"
    gen:
      go:
        out: "./db"
        package: "db"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: false
