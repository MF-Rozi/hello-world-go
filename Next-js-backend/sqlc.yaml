version: "2"
sql:
  - engine: "postgresql"
    schema: "./db/schema/"
    queries: "./db/queries/"
    gen:
      go:
        out: "./db/out"
        package: "db"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: false
